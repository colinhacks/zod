{
  "name": "@zod/datasets",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Generator for the @zod/*-codes data packages",
  "scripts": {
    "update": "nub src/update.ts",
    "test": "nub exec --node vitest run"
  }
}
